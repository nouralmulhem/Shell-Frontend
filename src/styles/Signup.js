import { Box } from '@mui/material';
import { styled } from '@mui/system';

export const NameTextField = styled(Box)(() => ({
  width: '80%',

  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'space-between',
  alignItems: 'center',
}));
