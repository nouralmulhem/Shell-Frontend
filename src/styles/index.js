import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    form: {
      // main: '#abcabc',
      // light: '#ffaaaa',
      // dark: '#abcfff',
      next: '#abcabc',
      back: '#abcfff',

    },
    progressbar: {
      main: '#1a90ff',
      gray: '#bdbdbd',
    },
  },
});

export default theme;
