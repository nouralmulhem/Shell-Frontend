import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    form: {
      // main: '#abcabc',
      // light: '#ffaaaa',
      // dark: '#abcfff',
      next: '#abcabc',
      back: '#abcfff',

    },
  },
});

export default theme;
